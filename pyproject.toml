[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyopentides"
version = "0.1.0"
description = "Tide predictions from official national hydrographic sources."
readme = "docs/pyopentides.md"
license = "MIT"
requires-python = ">=3.12"
authors = [{ name = "Dave O'Connor" }]
dependencies = ["aiohttp>=3.9"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/gerrowadat/open-tides"
Documentation = "https://github.com/gerrowadat/open-tides/blob/main/docs/pyopentides.md"
Changelog = "https://github.com/gerrowadat/open-tides/blob/main/CHANGELOG.md"
Issues = "https://github.com/gerrowadat/open-tides/issues"

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-asyncio",
  # Pins homeassistant exactly; bump deliberately.
  "pytest-homeassistant-custom-component==0.13.365",
  "ruff",
  "mypy",
]

[tool.uv]
# Home Assistant needs 3.14+; the library itself stays >=3.12.
environments = ["python_full_version >= '3.14.2'"]

[tool.hatch.build.targets.wheel]
packages = ["pyopentides"]

[tool.ruff]
target-version = "py312"
line-length = 88
extend-exclude = ["docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ANN", "RUF"]
ignore = ["ANN401"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["E501"]

[tool.ruff.lint.isort]
required-imports = ["from __future__ import annotations"]
known-first-party = ["pyopentides", "custom_components"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["pyopentides"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
